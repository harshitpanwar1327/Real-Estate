export class MediaModels {
    constructor(mediaData) {
        this.type = mediaData.type;
        this.propertyId = mediaData.propertyId;
        this.projectId = mediaData.projectId;
        this.cover = mediaData.cover;
        this.areaPlan = mediaData.areaPlan;
        this.images = mediaData.images;
    }
}